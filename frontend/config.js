// Configuration file - can be customized for different environments
// In Vercel, you can set environment variables and they'll be injected here
// For now, this is a simple way to configure the API URL

// Set your API URL here, or it will default to localhost
// In production, you can set this via Vercel environment variables
// and use a build script to inject it, or just hardcode it here
window.API_URL = window.API_URL || 'http://localhost:8000';

